const fs = require('fs');
const path = require('path');

module.exports = {
    add: function add (file, romset, selection) {

    },

    remove: function remove (file, selection) {
        
    }
};